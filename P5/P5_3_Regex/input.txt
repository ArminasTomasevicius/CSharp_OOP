void DuomenysInternet(Grybai & grybai)
{
    ifstream fd(u2);
//    string pav, tip;
//    GrybasInfo s1;
    int ns = 0;
    bool yra = true; 
    while(!fd.eof() && yra) {  // kol yra duomenø ir jie telpa á masyvà 
     fd >> pav >> tip; 
     s1.Dëti (pav, tip); 
     if(!fd.eof() && (ns - 1 < Grybai::CMax ) ) 
       grybai[ns++] = s1;      // áraðo naujà elementà
     else
       yra = false;
  }

  fd.close();
  grybai.Dëti(ns);
} 